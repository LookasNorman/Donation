<ul class="nav--actions">
    {% if not is_granted('ROLE_USER') %}
        <li><a href="{{ path('fos_user_security_login') }}">Zaloguj</a></li>
        <li class="highlighted"><a href="{{ path('fos_user_registration_register') }}">Załóż konto</a></li>
    {% else %}
        <li class="logged-user">
            {{ render(controller('AppBundle:User:loggedUserMenu')) }}
        </li>
        {% if is_granted('ROLE_ADMIN') %}
            <li class="btn--highlighted">
                <a href="{{ path('admin_page') }}">Panel administratora</a>
            </li>
        {% endif %}
    {% endif %}
</ul>