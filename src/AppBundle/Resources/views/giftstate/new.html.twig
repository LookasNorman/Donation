{% extends '@App/admin/base.html.twig' %}

{% block adminPage %}
    <h1>Giftstate creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('giftstate_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
