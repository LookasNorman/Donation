<div class="stats--item">
    <em>
        {% if donateInstitutions %}
            {{ donateInstitutions }}
        {% else %}
            0
        {% endif %}
    </em>
    <h3>{% trans %}home.donate.title{% endtrans %}</h3>
    <p><b>{% trans %}home.donate.subtitle{% endtrans %}:</b>
        {% for institution in institutions %}
            {{ institution.name }}
        {% endfor %}
    </p>
</div>
